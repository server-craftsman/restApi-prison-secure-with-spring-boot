# ============================================
# JWT Configuration
# ============================================

prison-secure:
  security:
    jwt:
      secret: ${JWT_SECRET:dev-secret-key-change-in-production}
      expiration: 86400000
      prisoner-expiration: 28800000
      issuer: prison-secure-system
      audience: prison-secure-users
